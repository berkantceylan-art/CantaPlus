import { ProductForm } from "@/components/admin/product-form"

/**
 * Yeni Ürün Sayfası (V2)
 * Build hatalarını önlemek için Sunucu tarafında başlar,
 * form mantığını ProductForm (Client) üzerinden yönetir.
 */
export default function NewProductPage() {
  return <ProductForm />
}
